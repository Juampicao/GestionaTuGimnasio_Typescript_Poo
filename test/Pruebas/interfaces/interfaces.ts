
export enum ISuscriptionUpdateMethod {
    TIME = "time",
    QUANTITY = "quantity", 
}

export enum IExpirationUpdateMethodTime {
    EXPIRATIONDATE = "expirationDate",
    LASTPAYMENT = "lastPayment",
    
}

export enum IExpirationUpdateMethodQuantity {
    AMOUNTUSES = "amountUses",
}

// TODO Cambiar Lso de arriba por los de abajo.

export enum ITypeSubscription {
    TIME = "time",
    QUANTITY = "quantity", 
}


export enum IUpdateMethodTime {
    EXPIRATIONDATE = "expirationDate",
    LASTPAYMENT = "lastPayment",
    MONTH = "month",
}

export enum IUpdateMethodQuantity {
    AMOUNTUSES = "amountUses",
}
